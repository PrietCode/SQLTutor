export const examples = [
  {
    id: 'select',
    label: 'SELECT columnas',
    level: 'Inicial',
    sql: 'SELECT customer_id, first_name, last_name, email\nFROM Customers;',
  },
  {
    id: 'where',
    label: 'WHERE y ORDER BY',
    level: 'Inicial',
    sql: 'SELECT name, price, stock\nFROM Products\nWHERE price > 70\nORDER BY price DESC;',
  },
  {
    id: 'logic',
    label: 'AND / OR',
    level: 'Inicial',
    sql: 'SELECT name, price, stock\nFROM Products\nWHERE (price > 100 AND stock > 10) OR stock = 0;',
  },
  {
    id: 'not',
    label: 'NOT',
    level: 'Inicial',
    sql: 'SELECT name, price, stock\nFROM Products\nWHERE NOT stock = 0;',
  },
  {
    id: 'like',
    label: 'LIKE',
    level: 'Inicial',
    sql: "SELECT first_name, last_name, city\nFROM Customers\nWHERE first_name LIKE 'A%';",
  },
  {
    id: 'group',
    label: 'GROUP BY + AVG',
    level: 'Intermedio',
    sql: 'SELECT category_id, COUNT(*) AS products, AVG(price) AS average_price\nFROM Products\nGROUP BY category_id\nHAVING COUNT(*) >= 2\nORDER BY average_price DESC;',
  },
  {
    id: 'sum',
    label: 'SUM + GROUP BY',
    level: 'Intermedio',
    sql: 'SELECT status, COUNT(*) AS orders, SUM(total) AS revenue\nFROM Orders\nGROUP BY status\nORDER BY revenue DESC;',
  },
  {
    id: 'aggregates',
    label: 'MIN / MAX / AVG',
    level: 'Intermedio',
    sql: 'SELECT MIN(price) AS cheapest, MAX(price) AS most_expensive, AVG(price) AS average\nFROM Products;',
  },
  {
    id: 'between',
    label: 'BETWEEN',
    level: 'Inicial',
    sql: 'SELECT name, price\nFROM Products\nWHERE price BETWEEN 70 AND 200;',
  },
  {
    id: 'in',
    label: 'IN',
    level: 'Inicial',
    sql: "SELECT first_name, city\nFROM Customers\nWHERE city IN ('Buenos Aires', 'Rosario');",
  },
  {
    id: 'null',
    label: 'IS NULL',
    level: 'Inicial',
    sql: 'SELECT first_name, last_name, email\nFROM Customers\nWHERE email IS NULL;',
  },
  {
    id: 'join-inner',
    label: 'INNER JOIN composición',
    level: 'Intermedio',
    sql: 'SELECT c.customer_id, c.first_name, o.order_id, o.total\nFROM Customers c\nINNER JOIN Orders o ON c.customer_id = o.customer_id\nORDER BY c.customer_id;',
  },
  {
    id: 'join-left',
    label: 'LEFT JOIN + NULL',
    level: 'Intermedio',
    sql: 'SELECT c.customer_id, c.first_name, o.order_id, o.status\nFROM Customers c\nLEFT JOIN Orders o ON c.customer_id = o.customer_id\nORDER BY c.customer_id;',
  },
  {
    id: 'join-right',
    label: 'RIGHT JOIN + NULL',
    level: 'Intermedio',
    sql: 'SELECT c.customer_id, c.first_name, i.import_id, i.source\nFROM Customers c\nRIGHT JOIN CustomerImports i ON c.customer_id = i.customer_id\nORDER BY i.import_id;',
  },
  {
    id: 'join-full',
    label: 'FULL JOIN completo',
    level: 'Intermedio',
    sql: 'SELECT c.customer_id, c.first_name, i.import_id, i.source\nFROM Customers c\nFULL JOIN CustomerImports i ON c.customer_id = i.customer_id\nORDER BY c.customer_id;',
  },
  {
    id: 'join-self',
    label: 'Self-Join empleados',
    level: 'Intermedio',
    sql: 'SELECT e.employee_id, e.name AS employee_name, m.name AS manager_name\nFROM Employees e\nLEFT JOIN Employees m ON e.manager_id = m.employee_id\nORDER BY e.employee_id;',
  },
  {
    id: 'subquery',
    label: 'Subconsulta WHERE',
    level: 'Avanzado',
    sql: 'SELECT name, price, stock\nFROM Products\nWHERE price > (\n  SELECT AVG(price)\n  FROM Products\n)\nORDER BY price DESC;',
  },
  {
    id: 'subquery-having',
    label: 'Subconsulta HAVING',
    level: 'Avanzado',
    sql: 'SELECT department, AVG(salary) AS avg_salary\nFROM Employees\nGROUP BY department\nHAVING AVG(salary) >= ALL (\n  SELECT AVG(salary)\n  FROM Employees\n  GROUP BY department\n);',
  },
  {
    id: 'exists',
    label: 'EXISTS',
    level: 'Avanzado',
    sql: 'SELECT first_name, last_name\nFROM Customers c\nWHERE EXISTS (\n  SELECT *\n  FROM Orders o\n  WHERE o.customer_id = c.customer_id\n)\nORDER BY first_name;',
  },
  {
    id: 'any',
    label: 'ANY / SOME',
    level: 'Avanzado',
    sql: 'SELECT name, price\nFROM Products\nWHERE price > ANY (\n  SELECT price\n  FROM Products\n  WHERE category_id = 2\n)\nORDER BY price DESC;',
  },
  {
    id: 'except',
    label: 'EXCEPT',
    level: 'Avanzado',
    sql: 'SELECT category_id\nFROM Products\nEXCEPT\nSELECT category_id\nFROM Products\nWHERE stock = 0;',
  },
  {
    id: 'date-conversion',
    label: 'YEAR + CAST',
    level: 'Intermedio',
    sql: "SELECT order_id, total, order_date\nFROM Orders\nWHERE YEAR(order_date) = 2026\n  AND order_date >= CAST('2026-03-01' AS DATE);",
  },
  {
    id: 'update',
    label: 'UPDATE',
    level: 'Intermedio',
    sql: "UPDATE Products\nSET stock = 10\nWHERE name = 'Blender';",
  },
  {
    id: 'insert',
    label: 'INSERT',
    level: 'Intermedio',
    sql: "INSERT INTO Categories (category_id, name)\nVALUES (4, 'Books');",
  },
  {
    id: 'delete',
    label: 'DELETE',
    level: 'Intermedio',
    sql: "DELETE FROM Orders\nWHERE status = 'cancelled';",
  },
];
